name: Fetch and Update Lake Data

on:
  schedule:
    - cron: "27 * * * *"  # Runs every hour at the 27th minute
  workflow_dispatch:  # Allows manual execution

permissions:
  contents: write  # Ensures the GitHub Action can push changes

jobs:
  fetch-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Ensures we get the full commit history

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run Python Script to Fetch Data
        run: python .github/scripts/fetch_lake_data.py

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Force Sync Local Branch with Remote
        run: |
          git fetch origin main
          git reset --hard origin/main  # Force update local branch to match remote
          git clean -fd  # Remove any untracked files

      - name: Commit and Push Changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git add .
          git commit -m "Updated lake data" || echo "No changes to commit"
          git push origin main
